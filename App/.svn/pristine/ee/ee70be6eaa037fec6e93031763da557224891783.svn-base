import tabs from './tabs'
import image from './image'
import dialog from './dialog'
const KesionUI = {
    install(Vue){
        Vue.use(tabs)
        Vue.use(image)
        Vue.use(dialog)
    }
}
export default KesionUI
